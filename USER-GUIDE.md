# Stock Loom 使用指南

本指南对应已发布的 0.2.0-beta.2 预览版，包含浏览器、现金、成交导入和额度展示等新增能力。Round 4 完整应用内升级验收仍在进行。

## 先认识界面

左边图标切换市场、我的股票、项目和 Scheduler 面板；设置在左下方。点击股票或项目文件，在中央标签页打开。右边 Copilot 用于提问和处理任务，顶部布局按钮可以调整信息页与对话区的显示；分隔线可拖动。

中央标签可右键关闭当前、其他、右侧或全部；键盘用 Shift+F10 打开菜单。批量关闭会保护未保存内容，不必逐页清理。

## 第一次配置

1. 打开设置 → 行情数据，填写 Tushare Token，等待保存结果。初始化目录和交易日历后搜索股票。接口提示无权限时，需要核对该接口授权，不能仅凭积分判断。
2. 打开设置 → 模型与连接，登录支持的账号或填写自己的模型服务连接。成功保存与实际连通是两件事；连接失败时按提示修改后重试。
3. 在市场面板查看指数或行业，或者在我的股票中搜索并添加自选。新页面会先显示已保存资料，再按需检查更新。数据日期与当前时间可能不同。

## 看一只股票

从搜索、自选、持仓或行业成分点击股票，打开同一详情标签。K线可选范围、缩放和拖动时间窗口；左右方向键查看交易日，Home/End 跳到当前窗口首尾。复权切换改变价格口径，不改变成交股数。

财务页选择利润表、资产负债表或现金流。趋势按同一累计报告期比较：半年度不能直接与全年比较。估值页可查看历史 PE、PB 和市值。“—”表示缺失，不是0；负值保留原含义。

公告页只在取得对应权限及数据后显示记录，点击原文打开受支持的披露来源。没有正文时，Copilot 不能把标题当作已读全文。旧快照仍可浏览；资料不足时先看日期、权限或重试提示。

## 管理自选与持仓

自选表示关注，不代表持有。持仓是本地手动记录，不连接券商下单。

在持仓 Tab 选择股票后，可调整总股数和每股成本；未知成本留空。交易记录区域用于记录一次买入或卖出，填写日期、本次股数、成交价和费用，无费用填0。确认的是本地记账，不是实际交易委托。

录错交易可在历史中更正或作废，原记录保留。超出当时持仓的卖出会被拒绝。旧版持仓显示为期初余额，不会凭空生成买入记录。

组合页区分剩余成本、浮盈与已实现盈亏。缺报价或成本时完整合计显示“—”，同时保留已知部分。行业占比按已估值持仓市值计算，不含现金；混合报价日期会标明。

也可以对 Copilot 说“把爱仕达加入自选”或“记录某股买入100股，价格10元，费用5元”。股数等必要信息缺失时需要补充。操作完成后查看持仓或自选核对结果。

## 使用 Copilot 与持续关注

在输入框选择模型、推理强度及权限；Enter发送，Shift+Enter换行，可附加文件。工具调用默认折叠，展开查看过程。向上翻阅时不会强制跳到底部，可用到底按钮返回。

Plan 与 Goal 状态显示在输入框上方。计划可以展开查看；目标的启动、停止和删除按状态条操作。股票资料仍由中央标签浏览，不需要先对话才能使用。

在 Scheduler 新建关注任务，填写内容、时区和频率。Cron 支持5字段或6字段（首位是秒）；固定间隔最少30秒。每次执行会使用模型连接，频率越高调用越多，并不代表数据源会有新数据。

任务可以暂停、编辑或删除。运行记录可打开对应会话轮次，失败或跳过后可手动重试。需要批准或回答时，打开记录处理待办。每个任务复用原会话，不会每次新增主会话。

关闭窗口选择后台运行时，调度继续；退出应用则停止，退出期间错过的触发不补跑。休眠或网络恢复时合并错过的触发，每次恢复至多安排一次补跑，不连续重放历史任务；上一轮未结束时跳过重叠。工作日是周一至周五，不自动排除交易所节假日。

任务提醒默认关闭。需要提醒时启用重要变化通知，并在任务内容中写清关注条件。提醒点击后定位关联会话；相同变化会去重，通知关闭不影响任务执行。

## 导入成交与记录现金

在持仓页展开“导入成交记录”，下载模板或选择 CSV，检查字段映射后预览。日期使用 YYYY-MM-DD，方向为 buy/sell；先修正错误行，再确认导入。保持同一来源的账本标识，重复导入会识别已有记录；修改过的同一记录不会被静默覆盖。

在现金账本填写日终余额作为起点，再记录后续入金、出金或额外费用。日终余额已经包含当天流水，系统从次日计入交易现金变化；成交记录里的费用不要再记一次。余额不知道时留空，不能用 0 代替。直接调整持仓股数不会凭空生成对应现金流水。

现金和所需报价都已知时才显示完整总资产。现金账本同样支持更正与作废，并保留原记录。

## 浏览网页与查看账户额度

在设置的浏览器分类启用“允许 Copilot 使用浏览器”。需要本机安装 Microsoft Edge；打开的是独立浏览器资料目录，不继承个人浏览器登录。需要登录时直接操作出现的窗口，完成后让 Copilot 继续。验证码、站点权限和付费访问仍由用户处理。

下载资料后让 Copilot 保存在项目内，并保留来源链接和采集时间。同名下载可能覆盖旧文件，应先归档已有资料。项目可预览 Markdown、图片、PDF、CSV/TSV 和 XLSX；表格不执行宏或公式，超限或不支持的文件可通过系统程序打开。

ChatGPT 账户额度位于设置和底部状态栏。它显示账户窗口的已用、剩余比例及重置时间，不是本次对话的费用。API 服务模式不提供 ChatGPT 账户额度；未登录或读取失败会显示不可用，不代表额度为零。真实登录账户的完整验收仍待完成。

## 保存资料、备份与更新

Copilot 可将后续需要复用的材料写入项目文件；用户想法可以写进项目 AGENTS.md 或 Skill。回答当前持仓时仍应读取软件中的最新记录，而不是沿用旧笔记。

设置 → 存储与备份提供资料库归档。它不包含模型凭证、Codex 会话或全部项目文件，详细范围见 [兼容与恢复](COMPATIBILITY.md)。恢复创建独立资料位置，保留原数据；不要直接让旧程序打开新版数据库。

设置 → 服务与更新可查看渠道、版本与更新状态。后台检查不会自动安装。会话运行中或草稿未保存时先完成当前工作，再安装；下载包会保留。旧公开 Beta 需要先手动引导升级，按 Releases 说明操作。Round 4 的完整应用内升级验收尚未完成；独立更新清单签名与 Windows 商业代码签名的区别见兼容说明。

## 提交使用反馈

请记录版本、操作入口、预期结果与实际结果。截图前遮盖凭证、私人持仓和会话。新用户验收应独立尝试看市场、个股、持仓、Copilot、定时关注和升级；开发者代操作不算新用户验收通过。
