# 兼容、备份与恢复

当前 Windows x64 v0.2.0-beta.3 已作为预览版发布，Round 4 尚未全部验收；以下说明该版本的兼容边界。

## 数据范围

| 内容 | 资料库备份 | 说明 |
| --- | --- | --- |
| 股票目录、自选、持仓与现金账本、数据库内快照和同步作业数据 | 包含 | SQLite 一致备份 |
| 数据库引用的日线、研究结果等受支持文件 | 包含 | 白名单路径、大小和哈希校验 |
| Tushare Token、模型密钥、登录凭证 | 不包含 | 使用应用凭证存储，不能靠资料库归档恢复 |
| Codex 项目文件、会话及本地调度配置 | 不包含 | 位于独立项目或应用目录 |
| 布局、草稿与其他应用偏好 | 不包含 | 不是整机或完整应用备份 |

更新安装保留已有用户目录，但必须通过真实升级验收验证；不能用数据库归档证明全部配置与会话均已恢复。迁移前的 SQLite 备份只保护数据库本身，不是完整资料归档。

## Schema 与恢复

本版 schema 为10，归档恢复接受5–10。恢复先检查归档清单、允许路径、大小、哈希、数据库一致性及引用文件，再创建独立资料目录，不覆盖原资料库。旧 schema 经现有迁移升级；schema8 的汇总持仓转为期初余额，原始汇总与迁移前备份保留，不伪造买卖历史。schema10新增现金账本，原账户现金保持未知，不假定为零。

支持读取旧归档不等于可以把新版数据库交给旧程序。不要直接用旧程序打开 schema10；需要旧版时使用与它兼容的独立备份和资料位置。恢复失败时原资料保留，失败产生的隔离目录不作为当前资料启用。

## 更新门槛

- 已公开的 Alpha/Beta 通过 Releases 手动下载安装。首次进入新更新机制仍需手动引导安装。
- 当前源码已固定 Ed25519 更新公钥，本地候选清单已签名并验签。清单绑定仓库、版本、兼容范围、包大小和 SHA256；不能靠改写清单跳过校验。
- 盈利前不要求商业 Windows 代码签名。当前程序与候选安装包均未签名时，允许在独立清单和完整性校验通过后安装；损坏或无效代码签名仍会拒绝。不得把这一策略解释为绕过 Windows 保护或具有受信发布者签名。
- 用户暂缓异机安装测试；无开发环境机器的安装、升级、卸载还没有验收，用户接受本版 Beta 保留这一已知限制。

## 已验证与未验证

自动化覆盖坏清单/坏包拒绝、版本与schema范围、取消下载、签名失败、迁移回滚、资料库备份恢复，以及模拟安装器失败后的服务恢复。隔离 Electron 覆盖更新状态、忙时暂缓、草稿保护与部分交互旅程。

独立安装标识的候选 A→B 安装测试已核对自选分组、现金、停用调度、研究文件、布局与加密凭证配置状态。它不证明正式安装标识的应用内升级、真实凭证可用性或完整模型会话保留，也不替代新用户独立使用验收。报告问题请附应用版本、操作步骤、界面错误及可公开日志，不上传凭证、持仓、私人会话或完整资料库。

## 盈利前发布签名策略（2026-09-12 用户调整）

Windows Authenticode 代码签名证书延后到正式盈利后，不作为盈利前 Alpha/Beta 发布前置条件。安装包未经 Windows 代码签名，不宣称受信任发布者。更新清单 Ed25519 签名独立于付费证书，当前已实现并用于公开预览版。正式源下载验证及完整升级验收按独立证据推进，不以发布成功代替验收。

## 旧版首次安装检查

本机已验证从 0.1.0-beta.1 手动安装 0.2.0-beta.2，原资料保留。旧 beta.1 卸载器可能卡在自身 PowerShell 探测，且该旧程序不包含新版的 `-NoProfile` 检查。此次验证曾核对精确父子进程并确认应用退出后结束挂起探测，由旧卸载器继续执行其内置 tasklist 检查；没有更改系统执行策略。不要将此恢复操作扩展成结束所有 PowerShell 或其他用户进程。后续 beta.2 起的安装器使用新版检查。


## Beta.6 开发边界（2026-09-13）

按维护策略调整，不再支持早期非正式版本的自动数据迁移。当前支持 schema 10；新资料库直接初始化为此格式，其它 schema 明确拒绝，原数据库不改写。当前 schema 10 的股票、自选、持仓与现金账本继续保留。第一轮研究与模型复盘执行链已移除，已有报告文件随备份保留。连接配置统一使用 ChatGPT 账户或自定义服务；早期独立 API 模式不再提供。详细清理及验证见 docs/runtime-cleanup.md。
