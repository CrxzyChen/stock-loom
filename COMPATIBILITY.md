# 兼容、备份与恢复

当前源码版本为 Windows x64 v0.1.0-beta.1，用户已接受功能收口；以下列明已验证机制及仍未执行的交付测试。

## 数据范围

| 内容 | 资料库备份 | 说明 |
| --- | --- | --- |
| 股票目录、自选、持仓账本、数据库内快照和同步作业数据 | 包含 | SQLite 一致备份 |
| 数据库引用的日线、研究结果等受支持文件 | 包含 | 白名单路径、大小和哈希校验 |
| Tushare Token、模型密钥、登录凭证 | 不包含 | 使用应用凭证存储，不能靠资料库归档恢复 |
| Codex 项目文件、会话及本地调度配置 | 不包含 | 位于独立项目或应用目录 |
| 布局、草稿与其他应用偏好 | 不包含 | 不是整机或完整应用备份 |

更新安装保留已有用户目录，但必须通过真实升级验收验证；不能用数据库归档证明全部配置与会话均已恢复。迁移前的 SQLite 备份只保护数据库本身，不是完整资料归档。

## Schema 与恢复

本版 schema 为9，归档恢复接受5–9。恢复先检查归档清单、允许路径、大小、哈希、数据库一致性及引用文件，再创建新的 sibling profile，不覆盖原资料库。旧 schema 经现有迁移升级；schema8 的汇总持仓转为期初余额，原始汇总与迁移前备份保留，不伪造买卖历史。

支持读取旧归档不等于可以把新版数据库交给旧程序。不要直接降级打开 schema9；需要旧版时使用与它兼容的独立备份和资料位置。恢复失败时原资料保留，失败产生的隔离目录不作为当前资料启用。

## 更新门槛

- Alpha1 通过 Releases 手动下载安装。首次进入新 updater 的引导仍需手动安装。
- 更新清单使用独立的 Ed25519 信任机制，安装包使用 Windows 代码签名；二者不能相互替代。
- 当前没有正式 Windows 签名证书，正式更新公钥也尚未部署。未签名包不能通过应用内可信安装流程，不提供跳过验证开关。
- 用户暂缓异机安装测试；无开发环境机器的安装、升级、卸载还没有验收，用户接受本版 Beta 保留这一已知限制。

## 已验证与未验证

自动化覆盖坏清单/坏包拒绝、版本与schema范围、取消下载、签名失败、迁移回滚、资料库备份恢复，以及模拟安装器失败后的服务恢复。隔离 Electron 覆盖更新状态、忙时暂缓、草稿保护与部分交互旅程。

这些证据不证明真实签名安装器已完成旧版到新版升级，也不替代新用户独立使用验收。报告问题请附应用版本、操作步骤、界面错误及可公开日志，不上传凭证、持仓、私人会话或完整资料库。

## 盈利前发布签名策略（2026-09-12 用户调整）

Windows Authenticode 代码签名证书延后到正式盈利后，不再作为盈利前 Alpha/Beta 发布的前置条件。盈利前交付须明确安装包未经 Windows 代码签名，不宣称受信任发布者。更新清单 Ed25519 签名独立于付费证书，仍保留来源及完整性校验；正式密钥部署与真实升级验收仍需完成。本次调整是交付文档的范围修订，不表示安装验证代码已经适配或安装验收已经通过。其他验收要求不变。
