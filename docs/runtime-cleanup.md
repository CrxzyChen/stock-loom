# 运行框架清理

2026-09-13，根据用户要求停止维护早期非正式版本兼容。当前代码进入 Beta.6 候选；此前 Beta.5 构建不包含本次清理，不能用于验证本次交付。

## 当前执行路径

- 会话、计划、目标及工具执行由 Codex app-server 管理。
- 应用只负责界面、配置、股票工具、资料服务及调度触发。
- 连接方式保留 ChatGPT 账户和自定义服务，移除早期单独的 OpenAI API 模式。

## 已移除

- 第一轮研究控制器、固定报告执行器、模型复盘控制器、复盘调度器及预算执行代码。
- 旧研究、复盘页面及其 preload / IPC / 数据服务 RPC 入口。
- 主进程在切换项目、备份、更新、退出时对上述旧执行器的等待与停止逻辑。
- 旧 worker 构建与分发入口；打包改为明确选择当前运行产物，避免历史 dist 文件混入。
- schema 1～9 自动迁移链。新资料库直接创建 schema 10，当前 schema 10 可以继续打开；其它版本明确拒绝，不改写原数据库。
- 早期 schema 备份恢复支持。新的更新清单仅声明当前数据格式。

## 保留边界

- 不删除用户资料、凭证、Codex 会话或已有股票记录。
- 已存历史报告作为归档文件备份，不再提供旧研究执行或专属交互。
- 当前仍在写入和使用的行情存储格式、通用网络错误处理、路径校验、账户隔离及当前数据备份继续保留。
- Codex 索引恢复是另一项操作，本次代码清理没有移动或重建真实会话索引。

## 验证

- TypeScript / Vue 类型检查通过。
- Node 回归：199 项通过；串行执行避免本机并发文件扫描引起的临时目录重命名失败。
- Python 回归：207 项，206 项通过、1 项因 Windows 符号链接权限跳过；重解析点拒绝测试通过。
- 新增当前 schema 创建、重新打开及 schema 1～9 拒绝时数据库字节不变验证。
- 当前用户股票资料库只读副本验证通过，135 个引用文件完整，未读取凭证。
- 当前运行包验证结果另见进度记录；整轮升级与最终交付仍未验收。


本机运行包验证完成：Beta.6已启动，当前用户schema 10、自选与持仓读取通过，preload不再暴露旧研究/复盘/API模型接口；app.asar中不存在dist/agent旧worker。冻结数据服务的持仓、现金盈亏、幂等写入、重启及备份恢复验证通过，使用合成资料与仅系统PATH。证据：validation/runtime-cleanup-app.json、validation/round4-packaged-cash.json、validation/round3-packaged-ledger.json。该运行包未覆盖已安装版本，也未发布。
