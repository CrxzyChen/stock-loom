const {app,ipcMain}=require('electron'),fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
const directory=fs.mkdtempSync(path.resolve('.runtime/tests/settings-categories-ui-'));app.setPath('userData',directory);app.disableHardwareAcceleration();
const evidence=JSON.parse(fs.readFileSync('validation/round2-history.json','utf8')),history=JSON.parse(fs.readFileSync(path.join(evidence.folder,'history.json'),'utf8'));
const record={passed:false,directory,realDesktop:true,historySource:'native captured test conversation',liveModel:false,reads:[]};
const handle=ipcMain.handle.bind(ipcMain);
// Only the history RPC response is replayed; the real preload, renderer,
// project initialization and persistence run unchanged in an isolated profile.
ipcMain.handle=(channel,listener)=>handle(channel,channel==='stock:copilot:list'?async()=>({data:[history.thread],nextCursor:null}):channel==='stock:copilot:read'?async(_event,p)=>{
 assert.equal(p.threadId,history.thread.id);record.reads.push(p.cursor);
 return {...history,thread:{...history.thread,turns:p.cursor?history.thread.turns.slice(0,1):history.thread.turns.slice(-1)},historyNextCursor:p.cursor?null:'older-fixture'};
}:listener);
const timer=setTimeout(()=>{record.error='timeout';fs.writeFileSync('validation/settings-categories-ui.json',JSON.stringify(record,null,2));app.exit(1)},45000);let started=false;
app.on('browser-window-created',(_,win)=>{if(started)return;started=true;win.webContents.once('did-finish-load',()=>void(async()=>{
 const js=s=>win.webContents.executeJavaScript(s),wait=async s=>{for(let i=0;i<120;i++){if(await js(s))return;await new Promise(r=>setTimeout(r,100))}throw Error('UI wait: '+s)};
 const click=async label=>{await wait(`Array.from(document.querySelectorAll('.copilot-panel button')).some(b=>b.textContent.trim()===${JSON.stringify(label)}&&!b.disabled)`);await js(`Array.from(document.querySelectorAll('.copilot-panel button')).find(b=>b.textContent.trim()===${JSON.stringify(label)}&&!b.disabled).click()`)};
 await wait(`document.querySelector('.history-thread')!==null`);
 await js(`document.querySelectorAll('.quick-layout button')[1].click()`);
 await js(`document.querySelector('.history-thread').click()`);
 await wait(`document.querySelector('.copilot-messages').textContent.includes('48271')`);
 await click('更早消息');await wait(`document.querySelector('.copilot-messages').textContent.includes('get_holdings')`);
 assert.equal(await js(`document.querySelectorAll('.copilot-messages .userMessage').length`),2);record.paginationRendered=true;
 await new Promise(resolve=>{win.webContents.once('did-finish-load',resolve);win.webContents.reload()});
 await wait(`document.querySelector('.copilot-messages').textContent.includes('48271')`);
 assert.ok(await js(`document.querySelector('.history-thread[aria-current=true]')!==null`));record.selectedConversationRestored=true;
 await js(`(()=>{const t=document.querySelector('.copilot-panel textarea');t.value='我的未发送草稿';t.dispatchEvent(new Event('input',{bubbles:true}))})()`);
 await js(`document.querySelector('[aria-label="收起历史会话"]').click()`);
 await wait(`document.querySelector('.copilot-history.collapsed')!==null`);
 assert.equal(await js(`document.querySelector('.copilot-panel textarea').value`),'我的未发送草稿');
 await new Promise(resolve=>{win.webContents.once('did-finish-load',resolve);win.webContents.reload()});
 await wait(`document.querySelector('.copilot-history.collapsed')!==null`);
 assert.equal(await js(`document.querySelector('.copilot-panel textarea').value`),'我的未发送草稿');
 await js(`document.querySelector('[aria-label="展开历史会话"]').click()`);
 await wait(`document.querySelector('.history-thread')!==null`);
 record.collapseAndDraftPersisted=true;
 win.setContentSize(1000,760);await new Promise(r=>setTimeout(r,200));
 assert.ok(await js(`document.documentElement.scrollWidth<=innerWidth`));
 assert.ok(await js(`document.querySelector('.copilot-conversation').getBoundingClientRect().width>=120`));record.narrowLayout=true;
 win.setContentSize(1440,900);await new Promise(r=>setTimeout(r,200));await js(`document.querySelector('.conversation-header button').click()`);await wait(`getComputedStyle(document.querySelector('.workarea')).display==='none'`);assert.equal(await js(`document.querySelector('.copilot-panel textarea').value`),'我的未发送草稿');
 await js(`document.querySelector('.conversation-header button').click()`);await wait(`getComputedStyle(document.querySelector('.workarea')).display!=='none'`);
 await js(`document.querySelector('.quick-layout button').click()`);await wait(`getComputedStyle(document.querySelector('.inspector')).display==='none'`);
 await js(`document.querySelectorAll('.quick-layout button')[2].click()`);await wait(`getComputedStyle(document.querySelector('.workarea')).display==='none'`);
 await js(`document.querySelector('.settings-entry').click()`);await wait(`document.querySelector('.work-tab.selected').textContent.includes('设置')`);assert.ok(await js(`getComputedStyle(document.querySelector('.workarea')).display!=='none'`));
 await js(`document.querySelectorAll('.quick-layout button')[1].click()`);await wait(`document.querySelector('.quick-layout button[aria-pressed=true]').textContent==='并排'`);
 assert.equal(await js(`document.querySelector('.appbar').textContent.includes('开发')`),false);assert.equal(await js(`document.querySelector('.appbar .connection')`),null);assert.ok(await js(`document.querySelector('.statusbar .connection')!==null`));
 assert.ok(await js(`document.querySelector('.settings-entry').getBoundingClientRect().top>document.querySelectorAll('.activity>button')[3].getBoundingClientRect().top`));record.layoutControls=true;record.settingsAndStatusMoved=true;
 await js(`document.querySelector('.tabs-conversation-toggle').click()`);await wait(`getComputedStyle(document.querySelector('.inspector')).display==='none'`);
 assert.equal(await js(`document.querySelector('.tabs-conversation-toggle').getAttribute('aria-label')`),'打开会话区');
 await js(`document.querySelector('.tabs-conversation-toggle').click()`);await wait(`getComputedStyle(document.querySelector('.inspector')).display!=='none'`);record.tabsHeaderToggle=true;
 const audit=await js(`Array.from(document.querySelectorAll('button.ui-action')).map(b=>({name:b.getAttribute('aria-label'),title:b.title,icon:!!b.querySelector('svg path'),only:b.classList.contains('ui-icon-only')}))`);assert.ok(audit.length>25);assert.ok(audit.every(b=>b.name&&b.title&&b.icon));record.iconControls=audit.length;record.iconNamesAndTooltips=true;
 await js(`document.querySelector('.tabs-conversation-toggle').focus()`);assert.ok(await js(`document.activeElement.matches('.tabs-conversation-toggle')`));record.focus=true;
 assert.equal(await js(`document.querySelectorAll('.settings-categories [role=tab]').length`),7);
 await js(`document.querySelector('#settings-category-data').click()`);await wait(`getComputedStyle(document.querySelector('#settings-pane-data')).display!=='none'`);
 await js(`(()=>{const input=document.querySelector('#token');input.value='unsaved-test-value';input.dispatchEvent(new Event('input',{bubbles:true}))})()`);
 await js(`document.querySelector('#settings-category-appearance').click()`);await wait(`getComputedStyle(document.querySelector('#settings-pane-data')).display==='none'`);
 await js(`document.querySelector('#settings-category-data').click()`);assert.equal(await js(`document.querySelector('#token').value`),'unsaved-test-value');record.unsavedInputPreserved=true;
 assert.equal(await js(`Array.from(document.querySelectorAll('.settings-category-pane')).filter(p=>getComputedStyle(p).display!=='none').length`),1);record.singleVisibleCategory=true;
 await js(`document.querySelector('#settings-category-data').dispatchEvent(new KeyboardEvent('keydown',{key:'ArrowDown',bubbles:true}))`);await wait(`document.activeElement.id==='settings-category-appearance'`);record.categoryKeyboard=true;
 await new Promise(resolve=>{win.webContents.once('did-finish-load',resolve);win.webContents.reload()});await wait(`document.querySelector('#settings-category-appearance')?.getAttribute('aria-selected')==='true'`);record.categoryRestored=true;
 await js(`document.querySelector('#settings-category-data').click()`);await wait(`getComputedStyle(document.querySelector('#settings-pane-data')).display!=='none'`);
 await new Promise(r=>setTimeout(r,250));record.screenshot=path.join(directory,'history.png');fs.writeFileSync(record.screenshot,(await win.webContents.capturePage()).toPNG());
 assert.ok(await js('document.documentElement.scrollWidth<=innerWidth'));assert.ok(await js(`document.querySelector('.copilot-history').getBoundingClientRect().left>=document.querySelector('.copilot-conversation').getBoundingClientRect().right-1`));record.historyOnRight=true;record.passed=true;
 clearTimeout(timer);fs.writeFileSync('validation/settings-categories-ui.json',JSON.stringify(record,null,2));app.quit();
})().catch(error=>{record.error=error.stack;clearTimeout(timer);fs.writeFileSync('validation/settings-categories-ui.json',JSON.stringify(record,null,2));app.exit(1)}));});
require(path.resolve('dist/main/main.cjs'));
