const {app,powerMonitor}=require('electron'),fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict'),cp=require('node:child_process');
const directory=fs.mkdtempSync(path.resolve('.runtime/tests/market-ui-sleep-'));cp.execFileSync(path.resolve('.venv312/Scripts/python.exe'),['scripts/seed-market-ui.py',directory],{windowsHide:true});app.setPath('userData',directory);app.disableHardwareAcceleration();
const children=[],spawn=cp.spawn;cp.spawn=function(command,args,options){const child=spawn.call(this,command,args,options);if(args?.[0]===path.resolve('apps/data-service/main.py')&&args.includes('--data-dir'))children.push(child);return child};
const record={passed:false,directory,realDesktop:true,syntheticData:true};let started=false;const timer=setTimeout(()=>finish(Error('No completed sleep/resume within 30 minutes')),1800000);
function finish(error){clearTimeout(timer);if(error)record.error=error.stack;else record.passed=true;fs.writeFileSync('validation/round2-sleep-live.json',JSON.stringify(record,null,2));app.exit(error?1:0)}
app.on('browser-window-created',(_,win)=>{if(started)return;started=true;win.webContents.once('did-finish-load',()=>void(async()=>{
 const js=s=>win.webContents.executeJavaScript(s,true),wait=async fn=>{for(let i=0;i<150;i++){if(await fn())return;await new Promise(r=>setTimeout(r,80))}throw Error('wait timeout')};
 await wait(()=>js(`window.stock.serviceStatus().then(x=>x.state==='ready')`));await js(`(()=>{const e=document.querySelector('#market-query');e.value='000001.SZ';e.dispatchEvent(new Event('input',{bubbles:true}));e.form.requestSubmit()})()`);await wait(()=>js(`document.querySelector('.stock-matches button')!==null`));await js(`document.querySelector('.stock-matches button').click()`);await wait(()=>js(`document.querySelector('.source-note')?.textContent.includes('130 个交易日')`));
 await js(`Array.from(document.querySelectorAll('button')).find(b=>b.textContent.includes('展开 Codex'))?.click()`);await js(`(()=>{const e=document.querySelector('.copilot-panel textarea');e.value='断开后保留此问题';e.dispatchEvent(new Event('input',{bubbles:true}))})()`);
 const tab=await js(`document.querySelector('[role=tab][aria-selected=true]').textContent`);record.phase='waitingForSleep';record.pid=process.pid;record.events=[];fs.writeFileSync('validation/round2-sleep-live.json',JSON.stringify(record,null,2));
 await js(`document.querySelector('.build-label').textContent='休眠验证 · 隔离测试资料'`);win.setTitle('Stock · 休眠验证');
 powerMonitor.once('suspend',()=>{record.events.push({event:'suspend',at:new Date().toISOString()});fs.writeFileSync('validation/round2-sleep-live.json',JSON.stringify(record,null,2))});
 powerMonitor.once('resume',()=>void(async()=>{assert.ok(record.events.some(e=>e.event==='suspend'));record.events.push({event:'resume',at:new Date().toISOString()});await wait(()=>js(`window.stock.serviceStatus().then(x=>x.state==='ready')`));assert.equal(await js(`document.querySelector('.copilot-panel textarea').value`),'断开后保留此问题');assert.equal(await js(`document.querySelector('[role=tab][aria-selected=true]').textContent`),tab);assert.equal((await js(`window.stock.barVersions('000001.SZ')`))[0].rows,130);record.viewAndDraftPreserved=true;record.snapshotPreserved=true;record.phase='completed';finish()})().catch(finish));

})().catch(finish));});require(path.resolve('dist/main/main.cjs'));
