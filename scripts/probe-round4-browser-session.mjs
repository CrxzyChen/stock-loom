// Interactive validation driver for the shipped native app-server/MCP boundary.
import fs from 'node:fs/promises';import path from 'node:path';import readline from 'node:readline';import {createHash} from 'node:crypto';
import {CodexTransport} from '../apps/desktop/src/main/codex-transport.mjs';import {ProcessGuard} from '../apps/desktop/src/main/process-guard.mjs';import {saveBrowserTools,browserToolOptions} from '../apps/desktop/src/main/browser-tools-settings.mjs';
const directory=await fs.mkdtemp(path.resolve('.runtime/web-research-')),home=path.join(directory,'home'),project=path.join(directory,'project');await fs.mkdir(home);await fs.mkdir(project);await saveBrowserTools(directory,true);
const binary=path.resolve('node_modules/@openai/codex-win32-x64/vendor/x86_64-pc-windows-msvc/bin/codex.exe'),guard=new ProcessGuard(path.resolve('.venv312/Scripts/python.exe'),[path.resolve('apps/data-service/main.py')],process.env);let transport;const record={directory,project,realCodex:true,modelTurns:0,steps:[]};
try{await guard.start();const browser=await browserToolOptions({directory,project,command:path.resolve('node_modules/electron/dist/electron.exe'),entry:path.resolve('node_modules/@playwright/mcp/cli.js')});transport=new CodexTransport({binary,binarySha256:createHash('sha256').update(await fs.readFile(binary)).digest('hex'),home,cwd:project,config:browser.config,env:browser.env,experimentalApi:true,protect:child=>guard.protect(child),timeoutMs:45000});transport.on('request',r=>transport.rejectRequest(r.id));await transport.start();const {thread}=await transport.request('thread/start',{cwd:project,approvalPolicy:'never',sandbox:'danger-full-access'});const list=await transport.request('mcpServerStatus/list',{threadId:thread.id});const server=list.data.find(s=>s.name==='stock_browser');if(!server)throw Error('browser tools missing');console.log(JSON.stringify({ready:true,project,tools:Object.keys(server.tools)}));const lines=readline.createInterface({input:process.stdin});for await(const line of lines){const command=JSON.parse(line);if(command.stop)break;const result=await transport.request('mcpServer/tool/call',{threadId:thread.id,server:'stock_browser',tool:command.tool,arguments:command.args??{}});record.steps.push({tool:command.tool,args:command.args,isError:!!result.isError,at:new Date().toISOString()});await fs.writeFile(path.join(directory,'last-result.json'),JSON.stringify(result,null,2));console.log(JSON.stringify(result).slice(0,12000))}
}catch(e){record.error=e.message;process.exitCode=1}finally{await transport?.stop();await guard.stop();await fs.writeFile('validation/round4-web-research-session.json',JSON.stringify(record,null,2));console.log(JSON.stringify({stopped:true,project,error:record.error}))}
